# Scripts

We did not use separate shell scripts for this project.
All experiments are run either:
- from the Colab notebooks in `notebooks/`, or
- using the Python modules in `src/` (e.g. `python -m src.train_codebert_task_b`).

This folder is kept only to match the standard project layout.
